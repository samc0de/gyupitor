version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./.bq_cache:/app/.bq_cache
      - ./bq_analyzer.db:/app/bq_analyzer.db
    command: uvicorn main:app --host 0.0.0.0 --port 8000
